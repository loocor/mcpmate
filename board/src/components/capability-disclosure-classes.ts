/**
 * Shared `<details>` / `<summary>` classes for server capability lists and audit raw-data rows.
 * Keep in sync wherever native disclosure markers must match (see CapabilityList).
 */
export const CAPABILITY_DETAILS_CLASS = "mt-2";
export const CAPABILITY_SUMMARY_CLASS = "cursor-pointer text-xs text-slate-500";
