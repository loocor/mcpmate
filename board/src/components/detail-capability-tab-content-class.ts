/** TabsContent for browser capability lists: fills remaining height; list scrolls inside the card. */
export const DETAIL_CAPABILITY_BROWSER_TAB_CONTENT_CLASS =
	"mt-0 flex min-h-0 flex-1 flex-col overflow-hidden data-[state=inactive]:hidden";
