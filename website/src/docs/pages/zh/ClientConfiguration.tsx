import DocLayout from "../../layout/DocLayout";
import { H2, P, Ul, Li } from "../../components/Headings";
import Callout from "../../components/Callout";

export default function ClientConfigurationZH() {
	return (
		<DocLayout meta={{ title: "配置管理", description: "决定 MCPMate 如何为客户端写入和选择能力来源" }}>
			<P>
				配置管理标签用于决定客户端的管理模式、能力来源、应用行为以及导入预览。它既控制未来想要的目标状态，也帮助你看清磁盘上的现状。
			</P>
			<H2>关键选择</H2>
			<Ul>
				<Li><strong>统一模式</strong> 适合希望通过内建 MCP / UCAN 工具进行会话内控制的场景。统一模式的直接暴露配置也在这里完成，并且只对统一模式会话生效。</Li>
				<Li><strong>托管模式</strong> 适合需要实时切换与更细控制的场景。</Li>
				<Li><strong>透明模式</strong> 适合必须直接写客户端配置文件的兼容场景，但会减少 MCPMate 的精细控制价值。</Li>
				<Li><strong>能力来源</strong> 决定托管模式或透明模式下，客户端跟随已激活配置集、所选共享配置集，还是客户端专属的自定义配置集。</Li>
			</Ul>
			<H2>治理放行与写入资格是两条线</H2>
			<Ul>
				<Li><strong>Allow / Deny</strong> 用来决定某个客户端是否允许进入 MCPMate 的能力放行圈，这是安全控制，不是配置编辑锁。</Li>
				<Li>即使客户端当前被禁行或暂停，你仍然可以继续调整它的管理模式、能力来源与基础元数据。</Li>
				<Li>真正把 Hosted、Unify 或 Transparent 的结果写入客户端自己的 MCP 配置文件，还要额外满足“已验证本地配置目标”这个条件。</Li>
				<Li>单纯保存治理状态不会凭空创建新的客户端配置文件，也不会把一个未验证路径自动升级成可信写入目标。</Li>
				<Li>如果应用时这些检查没有通过，MCPMate 会拒绝或延后写入，而不是在磁盘上留下只更新了一半的客户端配置。</Li>
			</Ul>
			<H2>三种模式真正意味着什么</H2>
			<Ul>
				<Li><strong>统一模式</strong> 初始只提供内建 MCP 工具，会在当前会话中通过内建流程浏览全局启用服务器的 capabilities，并在会话结束后自动重置。</Li>
				<Li><strong>托管模式</strong> 会让客户端只面对 MCPMate 提供的统一入口，因此 profile 切换、可见性控制与策略判断都还能保留在中间层。</Li>
				<Li><strong>透明模式</strong> 会把启用的 server 直接写进客户端自己的 MCP 配置，更适合兼容性或特殊场景。</Li>
			</Ul>
			<H2>来源选择适用于托管模式与透明模式</H2>
			<Ul>
				<Li><strong>统一模式</strong> 不使用这里的仪表板配置集选择。请在当前会话内通过内建 UCAN 工具浏览并调用来自全局启用服务器的 capabilities。</Li>
				<Li><strong>Activated</strong> 跟随全局当前已激活的配置集。</Li>
				<Li><strong>Profiles</strong> 允许某个客户端单独选择一组共享配置集，而不完全跟着全局活动集走。</Li>
				<Li><strong>Customize</strong> 会创建或复用这个客户端自己的专属配置集。</Li>
			</Ul>
			<H2>运行时策略边界</H2>
			<Ul>
				<Li><strong>托管模式</strong> 和 <strong>统一模式</strong> 都属于 MCPMate 托管的运行时模式。只要模式本身或其下配置细项发生变化，MCPMate 都会把它视为一次托管可见面更新：先持久化新状态，再刷新缓存与在线会话状态，并且只在托管可见能力面实际变化时发出能力列表通知。</Li>
				<Li><strong>透明模式</strong> 仍然会记录当前选择的模式与来源，但它的主要职责是把客户端原始 MCP 配置写到磁盘。完成这一步后，客户端会直接和上游 server 通信，而不是继续经过 MCPMate 托管运行时。</Li>
				<Li>因此，透明模式共享的是“配置写入路径”，而不是托管模式和统一模式那套运行时能力通知合同。</Li>
			</Ul>
			<H2>统一模式直接暴露（仅统一模式）</H2>
			<P>
				直接暴露只作用于统一模式，托管模式与透明模式的语义保持不变。
			</P>

			<Ul>
				<Li><strong>全部代理（默认）</strong>：所有已启用的服务器——包括已标记直达模式的服务器——仍通过内建 UCAN 工具完成访问。</Li>
				<Li><strong>服务器直达</strong>：选择已标记为统一模式可直接暴露的服务器，让该服务器的全部能力对客户端直接可见。</Li>
				<Li><strong>能力级直达（高级）</strong>：只直接暴露你选中的工具。v1 仅支持工具级直达；提示词、资源与模板仍保持代理路径。</Li>
			</Ul>

			<P>
				能力级直达现在会进入一个挂在客户端模块下的独立编辑页，而不是借用配置集详情页路由。这样既保留了与配置集工具页一致的批量编辑体验，也不会让侧边栏产生“正在修改配置集”的错觉，同时继续使用与其他客户端配置路径一致的治理感知应用检查。
			</P>

			<Callout type="warning" title="混合路由提示">
				能力级直暴露可能把同一条工作流拆成代理调用和直连调用。
				如果上游服务器依赖有状态的调用序列，混合路由可能带来意料之外的结果。
				MCPMate 会展示提示，但不会自动阻断或替你做安全边界判断。
			</Callout>

			<H2>推荐流程</H2>
			<Ul>
				<Li>先在 New / Edit 抽屉里声明这个客户端是否真的有本地 MCP 配置文件，并让 MCPMate 验证该路径，再把它作为写入目标使用。</Li>
				<Li>先在统一模式、托管模式、透明模式之间选定管理路径，再决定是否需要能力来源选择。</Li>
				<Li>只有在托管模式或透明模式下，才需要继续选择能力来源。</Li>
				<Li>覆盖写入前先看导入预览，避免误覆盖已有配置。</Li>
				<Li>应用前回到概览确认客户端已被正确检测。</Li>
			</Ul>
			<Callout type="warning" title="透明模式意味着不同的取舍">
				透明模式对兼容性很有帮助，但能力级开关的收益会下降，因为最终落到客户端的是更直接的服务器配置。
			</Callout>
			<Callout type="info" title="为什么托管模式会显得更强大">
				托管模式保留了 MCPMate 内建 profile / client 工具、按客户端计算可见性的逻辑，以及更丰富的策略控制。透明模式则是有意简化：它优先保证直接写配置，而不是保留这层运行时控制。
			</Callout>
		</DocLayout>
	);
}
