import Callout from "../../components/Callout";
import { H2, H3, Li, P, Ul } from "../../components/Headings";
import DocLayout from "../../layout/DocLayout";

export default function APIDocsJA() {
	return (
		<DocLayout
			meta={{
				title: "API ドキュメント",
				description: "RESTおよびMCP APIリファレンス",
			}}
		>
			<P>
				MCPMateは、そのRESTエンドポイントの対話型ドキュメントを<code>http://127.0.0.1:8080/docs</code>（バックエンドプロキシによって提供）で公開します。このページでは、ダッシュボードにショートカットを表示する方法と、そこに到達した後に何ができるかを説明します。
			</P>

			<H2>ショートカットの有効化</H2>
			<Ul>
				<Li>
					<strong>設定 → 開発者</strong>に移動し、<strong>APIドキュメントメニューを表示</strong>をオンにします。サイドバーの「設定」の近くに<em>API ドキュメント</em>リンクが表示されます。
				</Li>
				<Li>
					<strong>設定 → システム</strong>でバックエンドポートを変更した場合は、コピーしたコマンドでプロキシを再起動してください。サイドバーのリンクは引き続きデフォルトのAPIポートである<code>http://127.0.0.1:8080/docs</code>をターゲットとするため、デフォルト以外のポートを使用する場合はブックマークまたはURLを調整してください。
				</Li>
				<Li>
					リンクをクリックすると、新しいタブでドキュメントが開きます。ブックマークしたい場合は、直接URLにアクセスすることもできます。
				</Li>
			</Ul>

			<H2>ドキュメントの内容</H2>
			<H3>REST操作</H3>
			<P>
				OpenAPIビューには、エリア（システム、サーバー、プロファイル、ランタイム、マーケットプレイスなど）ごとにグループ化されたエンドポイントが一覧表示されます。操作を展開して、リクエスト/レスポンスのスキーマを検査し、「Try It」ボタンを使用してテスト呼び出しを発行します。
			</P>

			<H3>MCP トランスポートエンドポイント</H3>
			<P>
				ドキュメントでは、MCPトランスポートエンドポイント（Streamable HTTPの場合は<code>/mcp</code>）も参照しています。これらの概要を使用して、クライアント統合を作成する際のヘッダー、ペイロードキー、および認証の期待値を検証します。従来のSSEスタイルの構成はインポート時に正規化され、アクティブな構成ではStreamable HTTPとして表される必要があります。
			</P>

			<H2>ベストプラクティス</H2>
			<Ul>
				<Li>
					APIを試している間はダッシュボードを開いたままにして、表示される変更（サーバーのインストール、プロファイルの切り替えなど）を観察できるようにします。
				</Li>
				<Li>
					破壊的な操作をテストする場合は、他の人がシナリオを再現できるように、メモやスクリプトにリクエストをキャプチャします。
				</Li>
				<Li>
					将来のリリースで認証が必要になった場合は、<strong>Authorize</strong>ボタンを使用してください。ドキュメントツールは、通常のAPI呼び出しと同じCookieまたはトークンを再利用します。
				</Li>
			</Ul>

			<Callout type="warning" title="プロキシが実行中である必要があります">
				ドキュメントエンドポイントは、バックエンドが稼働している場合にのみ表示されます。ページが読み込まれない場合は、<code>cargo run -p app-mcpmate</code>（またはデプロイされたインスタンス）が<code>/docs</code>を提供しており、ブラウザが構成されたAPIポートにアクセスできることを確認してください。
			</Callout>
		</DocLayout>
	);
}
