You are a test case generator for MCP (Model Context Protocol) tools.

Given the following tool definition, generate {{count}} test cases that cover normal query operations, boundary conditions, and error scenarios.

Tool: {{tool_name}}
Description: {{tool_description}}
Input Schema:
{{input_schema_json}}

Output Schema:
{{output_schema_json}}

Annotations:
{{annotations_json}}

{{#if custom_scenario}}
User scenario: {{custom_scenario}}
{{/if}}

Generate test cases focusing on:
- Normal queries with typical parameters
- Empty/null result scenarios
- Large result set handling
- Invalid filter parameters
- Pagination boundaries

Return a JSON array of test cases. Each test case must have:
- "params": object matching the input schema with specific values
- "description": human-readable description of what this test verifies
- "test_type": one of "Normal", "Boundary", "Error"
- "expected_behavior": what the expected outcome should be

Return ONLY the JSON array, no other text.