# Details

After catalog discovery, inspect the capability details before reading. Details identify the published route, the expected template arguments, and the relevant scope.

Use only the canonical URI or template URI supplied by MCPMate. A Details response can provide a typed static ResourceLink or the information required to expand a template into a concrete URI. Do not substitute an upstream server URI into a standard read request.
