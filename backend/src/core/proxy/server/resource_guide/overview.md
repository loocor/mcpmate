# Resource Guide Overview

MCPMate publishes active Surface resources through standard lists for the current consumer. BrokerOnly resources instead come from Catalog and Details.

Use `resources/read` for either source: a typed static ResourceLink or an expanded template concrete URI.

Use this guide when you need the stable MCPMate workflow rather than a server-specific resource description.
