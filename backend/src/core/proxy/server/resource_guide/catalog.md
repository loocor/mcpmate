# Catalog

The catalog is the discovery step for available Resource capabilities. It can contain more capability knowledge than the active Surface exposes through standard lists.

Treat the active Surface lists as the source of standard Resource operations for the current consumer. BrokerOnly capability knowledge is not a total directory returned by those lists.
