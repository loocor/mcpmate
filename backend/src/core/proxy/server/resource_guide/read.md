# Standard Read

Use `resources/read` with an Active Surface URI from standard lists or a BrokerOnly URI from Catalog and Details. Both a typed static ResourceLink and an expanded template concrete URI use the same standard read request.

For a guide topic, retain the `/template/` segment in `mcpmate://resources/template/mcpmate/guide/{topic}` and replace `{topic}` with one of the documented stable topics.
